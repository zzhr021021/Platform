#include<bits/stdc++.h>
using namespace std;

char pid;

bool istrue(ll x){
    string s = to_string(x);
    if(s.size() % 2 == 0){

    }
}

int main() {
    cin >> pid;
    if(pid == 'A')
        puts("4430091");
    else
        puts("Answer for Problem B");
    return 0;
}